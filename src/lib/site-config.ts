// Edits to this file require a dev-server restart (or a new deploy in
// production) to appear on the site — see CLAUDE.md's "Running" section
// for why: values here are read once at module load, not per-request.
export const SITE_TITLE = "MultiBlog";
